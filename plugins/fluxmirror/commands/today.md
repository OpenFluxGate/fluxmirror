---
description: Alias for /fluxmirror:report-today
---

Invoke the canonical skill via the Skill tool:

```
Skill(skill="fluxmirror:report-today")
```

Pass no arguments. Output its result verbatim — do NOT re-derive the
behavior or paraphrase. This guarantees `/fluxmirror:today` and
`/fluxmirror:report-today` produce byte-identical output.
