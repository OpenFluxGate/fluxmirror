// fluxmirror doctor — health check.
//
// STEP 8 will implement: db connectivity, agent integrations, wrapper
// engine, version drift, filesystem permissions, legacy path migration.

use std::process::ExitCode;

pub fn run() -> ExitCode {
    eprintln!("fluxmirror doctor: not yet implemented (STEP 8)");
    ExitCode::from(2)
}
