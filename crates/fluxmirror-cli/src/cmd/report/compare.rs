// fluxmirror compare — today vs yesterday side-by-side.
//
// M1 stub. Filled in by a subsequent slice.

use std::process::ExitCode;

pub fn run() -> ExitCode {
    eprintln!("fluxmirror compare: not yet implemented (M1)");
    ExitCode::from(2)
}
