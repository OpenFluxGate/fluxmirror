// fluxmirror agent — single-agent filtered report.
//
// M1 stub. Filled in by a subsequent slice.

use std::process::ExitCode;

pub fn run() -> ExitCode {
    eprintln!("fluxmirror agent: not yet implemented (M1)");
    ExitCode::from(2)
}
