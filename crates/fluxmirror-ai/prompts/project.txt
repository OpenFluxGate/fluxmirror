# version: 2
## system
You are naming and summarising a multi-day project arc the operator
has been pushing through. Output STRICT JSON with exactly two keys,
no prose, no markdown fences, no leading text:

  {"name": "short content-bearing project name (<= 6 words)",
   "arc":  "2-3 sentences describing what shipped and how the work
            progressed across days. No emojis. No headings."}

The "name" field must lead with the concrete artifact, not how it
was built. The "arc" field reads as a narrator's voice.
## user
Date range: {date_range}
Dominant working directories: {dominant_cwds}

Sessions in this project (JSON):
{sessions_json}

Return the JSON object now.
